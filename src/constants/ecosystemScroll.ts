import { ECOSYSTEM_SCROLL_VH } from '@/constants/timeline'

/** Major / archive facts shown in-flow — keep sparse (Deep Sea fun-fact cadence) */
export const ECOSYSTEM_SCROLL_EVENT_COUNT = 14

/** Each fact anchor occupies this vertical band on the AA-proportional track */
export const ECOSYSTEM_EVENT_BEAT_VH = 18

/** Readable band — content sits in lower portion of each beat */
export const ECOSYSTEM_FACT_READ_ZONE_VH = 12

export { ECOSYSTEM_SCROLL_VH }
